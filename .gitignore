# =============================================================================
# DockerRescueKit — .gitignore
# =============================================================================

# ── Secrets and local runtime data ───────────────────────────────────────────
.env
.env.*
!.env.example
.local-data/
secrets.json
*.key
*.pem

# ── License server lives in a SEPARATE PRIVATE REPO ──────────────────────────
# Holds the RS256 JWT signing private key + Square API tokens. MUST NOT be
# committed here under any circumstance. Real repo at:
#   K:\Projects\DRK_LicenseServer\
# Push target: private GitHub repo under personal account (NOT the gozippy org).
/license-server/

# ── Log and checkpoint output (generated at runtime) ─────────────────────────
WSL_Update_Logs/
DockerCheckpoints/
*.log
*.log.*

# ── Backup archives (can be very large) ──────────────────────────────────────
*.tar
*.tar.gz
*.tgz
*.zip
docker-backups/
backup-temp-*

# ── Node / npm ────────────────────────────────────────────────────────────────
node_modules/
dist/
!packages/shared/dist
build/
.cache/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Test and coverage artifacts ───────────────────────────────────────────────
coverage/
test-results/
playwright-report/
e2e-results/
/tmp/

# ── SQLite databases (generated at runtime) ───────────────────────────────────
*.sqlite
*.sqlite3
*.db

# ── AI agent and IDE private configuration ───────────────────────────────────
# Private workflow tooling — not useful to end users.
.autoclaw/
.clinerules/
.agent/
.qodo/
.kilocodemodes
.claude/rules/cross-agent-protocol.md

# ── OS generated ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ── Editor ────────────────────────────────────────────────────────────────────
.vscode/settings.json
.vscode/launch.json
.idea/
*.suo
*.user
*.swp
*.swo
*.tmp
*~

# ── Third-party license raw machine output (path-leaking) ────────────────────
THIRD_PARTY_LICENSES.json

# ── Generated screenshots (track only what's referenced in README) ───────────
# (docs/screenshots/ is intentionally tracked — it's the README/Marketplace assets)

# ── Misc IDE / harness leftovers ─────────────────────────────────────────────
.aider*
.cursorignore
.continueignore
.windsurfignore

# Claude Code worktrees (embedded git repos)
.claude/worktrees/
